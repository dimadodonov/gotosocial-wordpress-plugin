# Как создать релиз

Это руководство описывает процесс создания нового релиза плагина GoToSocial Widget.

## Подготовка к релизу

### 1. Обновление версии

Обновите номер версии в следующих файлах:

#### `gotosocial.php`
```php
* Version: X.Y.Z
```

И в методе `enqueue_assets()`:
```php
'1.X.Y'  // для версий CSS и JS файлов
```

И на странице настроек в методе `render_settings_page()`:
```php
v1.X.Y
```

### 2. Обновление CHANGELOG.md

Добавьте новую секцию в начало файла `CHANGELOG.md`:

```markdown
## [X.Y.Z] - YYYY-MM-DD

### ✨ Новые возможности
- Описание новой функции

### 🐛 Исправления
- Описание исправленного бага

### 🎨 Улучшения
- Описание улучшения
```

### 3. Обновление README-GITHUB.md

Обновите badge с версией:
```markdown
[![Version](https://img.shields.io/badge/version-X.Y.Z-blue.svg)]
```

### 4. Проверка кода

Убедитесь, что:
- ✅ Код работает корректно
- ✅ Нет PHP ошибок
- ✅ JS работает без ошибок в консоли
- ✅ CSS отображается корректно
- ✅ Все настройки сохраняются и применяются
- ✅ Виджет работает на разных разрешениях экрана

## Создание релиза на GitHub

### Способ 1: Автоматический (рекомендуется)

1. **Закоммитьте все изменения:**
   ```bash
   git add .
   git commit -m "Release v1.X.Y"
   git push origin main
   ```

2. **Создайте и запушьте тег:**
   ```bash
   git tag v1.X.Y
   git push origin v1.X.Y
   ```

3. **GitHub Actions автоматически:**
   - Создаст ZIP архив плагина
   - Создаст релиз на GitHub
   - Прикрепит ZIP файл к релизу
   - Добавит описание из CHANGELOG.md

4. **Проверьте релиз:**
   - Откройте https://github.com/dimadodonov/gotosocial-wordpress-plugin/releases
   - Убедитесь, что релиз создан
   - Скачайте и протестируйте ZIP файл

### Способ 2: Ручной

1. **Закоммитьте все изменения:**
   ```bash
   git add .
   git commit -m "Release v1.X.Y"
   git push origin main
   ```

2. **Создайте ZIP архив вручную:**
   ```bash
   # Из директории плагина
   zip -r gotosocial.zip . \
     -x "*.git*" \
     -x "*node_modules*" \
     -x "*.DS_Store*" \
     -x "*build*"
   ```

3. **Создайте релиз на GitHub:**
   - Перейдите на https://github.com/dimadodonov/gotosocial-wordpress-plugin/releases
   - Нажмите "Draft a new release"
   - Tag version: `v1.X.Y`
   - Release title: `GoToSocial Widget v1.X.Y`
   - Описание: скопируйте из CHANGELOG.md
   - Прикрепите ZIP файл
   - Нажмите "Publish release"

## После публикации релиза

### 1. Проверка автообновлений

Установите плагин на тестовом сайте и проверьте:
- Отображается ли уведомление об обновлении
- Корректно ли работает процесс обновления
- Сохраняются ли настройки после обновления

### 2. Обновление документации

Если необходимо, обновите:
- README.md
- README-GITHUB.md
- Скриншоты (если изменился интерфейс)

### 3. Уведомление пользователей

Если у вас есть список пользователей, уведомите их о:
- Новых возможностях
- Важных исправлениях
- Инструкциях по обновлению (если требуется)

## Семантическое версионирование

Плагин использует [Semantic Versioning](https://semver.org/lang/ru/):

- **MAJOR** (X.0.0) - несовместимые изменения API
- **MINOR** (0.X.0) - новая функциональность, обратно совместимая
- **PATCH** (0.0.X) - исправления ошибок, обратно совместимые

### Примеры:

- `1.0.0` → `1.0.1` - исправление бага
- `1.0.1` → `1.1.0` - новая функция
- `1.9.0` → `2.0.0` - Breaking changes

## Откат релиза

Если обнаружена критическая ошибка:

1. **Удалите релиз на GitHub:**
   - Перейдите в Releases
   - Откройте проблемный релиз
   - Нажмите "Delete"

2. **Удалите тег:**
   ```bash
   git tag -d v1.X.Y
   git push origin :refs/tags/v1.X.Y
   ```

3. **Исправьте ошибку и создайте новый релиз**

## Чеклист перед релизом

- [ ] Обновлены все версии в коде
- [ ] Обновлен CHANGELOG.md
- [ ] Обновлены README файлы
- [ ] Протестирован функционал
- [ ] Закоммичены все изменения
- [ ] Создан и запушен тег
- [ ] Релиз опубликован на GitHub
- [ ] ZIP файл прикреплен к релизу
- [ ] Протестированы автообновления
